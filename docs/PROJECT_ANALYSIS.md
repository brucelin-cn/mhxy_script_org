# mhxy_script_org 项目分析

## 1. 项目定位

这是一个面向 Windows 环境的 Python 自动化脚本项目，目标是为《梦幻西游：时空》相关 PC/桌面端窗口提供半自动或全自动操作能力。

项目的核心思路不是调用游戏 API，而是基于以下手段做界面级自动化：

- 通过窗口标题获取目标游戏窗口
- 基于截图模板做界面元素识别
- 将固定坐标和相对坐标映射到窗口内
- 通过鼠标点击、拖拽、键盘输入完成任务流程

从代码组织看，这个仓库已经不是单脚本集合，而是一个“自动化内核 + 多个任务脚本 + PyQt 控制台 + 图片资源库”的完整工具仓库。

## 2. 技术栈

后端/脚本层：

- Python 3.8+
- `PyAutoGUI`：截图、窗口、鼠标键盘控制
- `pydirectinput`：键盘输入补充
- `opencv-contrib-python`：模板匹配置信度支持
- `Pillow` / `numpy`：图像相关依赖
- `playsound`：异常或超时提醒
- `psutil`：进程探测、启动/停止游戏
- `requests`：部分远程控制场景依赖

桌面 UI：

- `PyQt6`
- `qtpy`

运行平台特征：

- 明显偏向 Windows
- 依赖桌面窗口、屏幕分辨率、焦点切换
- 对 Mac/Linux 基本没有兼容设计

## 3. 目录结构

```text
.
├─ mhxy/                    基础运行时与通用工具
├─ resources/               各任务用到的截图模板、配置、说明文件
├─ ui/                      PyQt 图形界面
├─ game_process.py          窗口缩放/排布工具
├─ game_conf.py             游戏程序路径配置读取
├─ game_exehelp.py          游戏进程启动/停止辅助
├─ game_watcher.py          断线/重登监控雏形
├─ pic_target.py            图片目标与点击策略抽象
├─ mhxy_*.py                各类任务脚本
└─ README.md                现有说明，当前存在编码可读性问题
```

## 4. 核心架构

### 4.1 运行内核：`mhxy/__init__.py`

这是全项目最核心的文件，承担了大部分基础设施职责：

- 日志初始化，输出到 `mhxy_script.log`
- 全局窗口框架定义：`frame`、`frameSize`、`originSize`、`smallSize`
- 窗口内相对坐标换算
- 基于图片的查找与点击封装
- 战斗中/战斗后流程辅助
- 任务跳转、活动面板打开、背包使用等公共动作
- `MhxyScript` 基类，用于统一窗口初始化和停止控制

这个文件本质上相当于项目的“自动化 SDK”。

### 4.2 目标模型：`pic_target.py`

这个模块把“识别图片并点击”的行为抽象成结构化对象：

- `PicTarget`：描述一张图、识别方式、点击方式
- `PicLocateType`：整块定位或中心点定位
- `PicClickType`：按右下角、按中心点或自定义偏移点击
- `tryClickPic()`：识别成功后执行点击

这让 `mhxy_common.py` 和部分任务脚本可以用声明式方式配置流程。

### 4.3 窗口管理：`game_process.py`

这个脚本用于统一游戏窗口布局，方便多开和脚本坐标复用：

- 将窗口缩放到 `smallSize=(907, 707)` 或还原到 `originSize=[1040, 807]`
- 横向/纵向排列多个窗口
- 通过索引只调整某个目标窗口

它是很多任务脚本能稳定运行的前置条件。

### 4.4 配置与进程管理

相关文件：

- `game_conf.py`
- `game_exehelp.py`
- `game_watcher.py`

职责分别是：

- 从 `resources/common.ini` 读取游戏 EXE 路径、启动器路径、玩家配置
- 启动/停止游戏进程
- 监测断线并尝试重登

其中 `game_watcher.py` 更像“监控雏形”，有一定设计意图，但实现还不够完整。

### 4.5 图形界面：`ui/`

`ui/mhxy_pyqt.py` 是主入口，功能包括：

- 提供按钮式启动各类脚本
- 配置不同任务的参数弹窗
- 选择目标窗口
- 调整游戏窗口大小
- 打开日志
- 执行自定义脚本

UI 本质上是脚本启动器，而不是业务逻辑核心。

## 5. 业务脚本分层

### 5.1 日常任务类

代表文件：

- `mhxy_richang.py`
- `mhxy_baotu.py`
- `mhxy_mijing.py`
- `mhxy_dati.py`
- `mhxy_yabiao.py`
- `mhxy_shimeng.py`

特征：

- 通常以“进入活动面板 -> 找到任务 -> 点击进入 -> 循环识别界面状态 -> 执行动作”为主
- 支持多个任务串行
- 常使用 `gotoActivity()` 进入活动

其中 `mhxy_richang.py` 是聚合入口，负责串联宝图、秘境、答题、押镖、师门等任务。

### 5.2 副本/多角色联动类

代表文件：

- `mhxy_fuben.py`
- `mhxy_520.py`

特征：

- 偏向连续流程执行
- 会依赖战斗状态判断、剧情跳过、任务追踪
- `mhxy_520.py` 会把副本和抓鬼串起来，形成更长链路

### 5.3 单任务高频循环类

代表文件：

- `mhxy_ghost.py`
- `mhxy_bangpai2.py`
- `mhxy_mine.py`
- `mhxy_linlongshi.py`
- `mhxy_menpai.py`
- `mhxy_haidi.py`
- `mhxy_mihunta.py`

特征：

- 一般从配置文件读取参数
- 循环执行直到达到轮数、超时或手动中止
- 对窗口尺寸、位置、任务栏位置比较敏感

### 5.4 工具类

代表文件：

- `mhxy_auto_battle.py`
- `mhxy_hanhua.py`
- `mhxy_shopping.py`
- `mhxy_shopping2.py`
- `mhxy_shopping3.py`
- `mhxy_remote_control.py`
- `mhxy_remote_client.py`

用途：

- 自动战斗
- 购物/蹲商品
- 远程触发任务
- 文本输入或辅助操作

## 6. 执行流程模型

从代码抽象上看，项目大多数脚本都遵循同一套路：

1. 通过 `init(idx=...)` 绑定某个游戏窗口
2. 读取窗口尺寸，建立窗口内坐标系
3. 根据任务需要选择是否缩放到小窗口
4. 通过 `gotoActivity()` 或固定入口进入任务
5. 循环查找图片资源
6. 按图片类型和点击策略执行动作
7. 用战斗状态、完成标识、超时机制决定是否继续

这套模型的优点是复用度高，缺点是对界面变化非常脆弱。

## 7. 配置体系

### 7.1 全局配置

`resources/common.ini`

当前用于保存：

- 游戏主进程路径 `path`
- 启动器路径 `launcher`
- 玩家列表 `players`

### 7.2 任务级配置

示例：

- `resources/ghost/ghost.ini`
- `resources/richang/richang.ini`
- `resources/menpai/menpai.ini`
- `resources/mihunta/mihunta.ini`
- `resources/linlongshi/linlongshi.ini`
- `resources/bangpai/small/bangpai.ini`

这些配置文件通常保存：

- 任务追踪位置
- 循环次数
- 是否调整窗口大小
- 预警时间
- 特定任务参数

### 7.3 UI 配置

`ui/script.ini`

当前用于保存：

- 脚本根目录
- 上次执行的自定义脚本命令

## 8. 资源组织方式

`resources/` 是整个仓库的核心资产之一。

目录按任务拆分，例如：

- `resources/common/`：通用按钮、登录、关闭、确认等
- `resources/richang/`：活动入口相关
- `resources/ghost/`：抓鬼任务
- `resources/fuben/`：副本相关
- `resources/baotu/`：宝图相关
- `resources/shop/`：购物相关
- `resources/mine/`：挖矿相关

这些图片模板直接决定脚本稳定性。仓库中的代码和资源是强耦合关系，不能只迁移代码不迁移资源。

## 9. 代表性入口

如果从“怎么用”角度看，当前几个最重要的入口是：

- `ui/mhxy_pyqt.py`
  - 图形界面总入口，适合人工操作
- `game_process.py`
  - 先把窗口排布到脚本适配状态
- `mhxy_richang.py`
  - 单人日常聚合入口
- `mhxy_520.py`
  - 多任务串行入口
- `mhxy_ghost.py`
  - 抓鬼代表脚本
- `mhxy_common.py`
  - 通用任务引擎试验入口

## 10. 当前项目的优点

- 自动化基础能力比较集中，复用性不错
- 资源目录划分清楚，便于按任务维护模板
- 基本具备多开窗口支持
- 有图形界面，降低了非代码用户使用门槛
- 配置文件外置，部分参数无需改代码
- 已经形成“通用能力 + 任务脚本”的雏形

## 11. 当前项目的主要问题

### 11.1 文档与编码问题

- 仓库现有 `README.md` 存在明显编码可读性问题
- 部分中文字符串在代码输出中也有乱码痕迹
- 新接手者很难仅靠现有说明完成环境搭建

### 11.2 全局状态过多

`mhxy/__init__.py` 使用了大量全局变量：

- `frame`
- `frameSize`
- `frameSizeCm`
- 日志对象

这使得：

- 多窗口并行时容易相互影响
- 模块测试困难
- 调用顺序必须非常谨慎

### 11.3 代码耦合较高

主要表现为：

- 业务脚本直接依赖图片路径与硬编码坐标
- 任务逻辑、界面识别、等待策略写在同一层
- 很少有可复用的状态机抽象

一旦游戏 UI 改版，维护成本会比较高。

### 11.4 错误处理不统一

虽然项目有日志和声音提醒，但整体仍偏“脚本式容错”：

- 很多地方只循环等待，没有明确失败分支
- 异常恢复逻辑零散
- 断线重连能力有雏形，但没有形成统一机制

### 11.5 命名与一致性问题

例子：

- `mhxy_shopping.py` 存在，但 UI 中调用的是 `mhxy_shopping1`
- `getTarget()` 中窗口索引映射存在跳号现象
- `game_watcher.py`、`game_exehelp.py` 中有部分返回值/流程不完整
- 同类任务脚本参数风格不完全统一

这些问题会增加后续维护成本。

### 11.6 大日志文件未忽略

仓库根目录存在 `mhxy_script.log`，且体积很大。

这说明：

- 运行日志可能被直接纳入工作区
- `.gitignore` 目前没有覆盖日志文件
- 长期会影响仓库体积与协作体验

## 12. 维护建议

按投入产出比排序，建议优先做下面几件事。

### 12.1 先修文档和仓库卫生

- 修复 `README.md` 编码
- 补一份清晰的使用说明
- 把 `mhxy_script.log`、临时截图、打包产物加入 `.gitignore`

### 12.2 把“窗口上下文”对象化

建议把下面这类全局状态收敛到单个上下文对象中：

- 窗口句柄
- frame 坐标
- frameSize
- 配置

这样可以明显降低多窗口互相污染的风险。

### 12.3 把识别流程抽成状态机

当前很多任务是“无限循环 + if/else 查图点击”。

后续可以拆成：

- 进入任务
- 对话处理
- 战斗等待
- 领取奖励
- 结束退出

这样脚本会更稳定，也更容易诊断失败在哪一步。

### 12.4 统一脚本入口协议

建议所有脚本统一参数风格，例如：

- `-i / --idx`
- `-ir / --idx-array`
- `-m / --mission`
- `--resize`
- `--shutdown`

统一后，UI 和 CLI 都更容易维护。

### 12.5 对资源做版本化管理

建议为关键模板建立最基本的说明：

- 这个图对应什么界面
- 建图时窗口尺寸是多少
- 推荐识别阈值是多少
- 如果失效应该在哪个流程重新截图

这会显著降低“资源失效但没人知道该怎么修”的问题。

## 13. 适合接手者的阅读顺序

如果后续要继续维护，建议按下面顺序读代码：

1. `mhxy/__init__.py`
2. `pic_target.py`
3. `game_process.py`
4. `mhxy_richang.py`
5. `mhxy_baotu.py`、`mhxy_ghost.py`、`mhxy_fuben.py`
6. `ui/mhxy_pyqt.py`

这个顺序能先理解“底层能力”，再看“具体任务”，最后看“UI 如何调度脚本”。

## 14. 结论

这是一个已经沉淀出实际可用能力的游戏界面自动化项目，不是简单 demo。

它的强项在于：

- 对具体任务场景积累了较多图片资源和脚本经验
- 具备多脚本、多任务、多窗口协同基础
- 有 UI 包装，实际可操作性强

它的短板也很明确：

- 代码偏脚本化，工程化不足
- 全局状态重
- 对分辨率、窗口排布、图片模板高度敏感
- 文档和一致性还有较大提升空间

如果后续目标是“继续个人使用”，当前结构还能支撑一段时间。
如果后续目标是“长期维护或交接”，优先级最高的工作是补文档、收敛全局状态、统一脚本协议。

## 15. 代码分层的真实情况

虽然仓库表面上分成了 `mhxy/`、`ui/`、`resources/` 和若干 `mhxy_*.py` 脚本，但从调用关系看，当前项目更接近下面这 5 层。

### 15.1 第 0 层：运行环境假设

这层没有显式代码文件，但几乎所有脚本都依赖它：

- 必须有可见的 Windows 桌面窗口
- 游戏窗口标题要能被 `getWindowsWithTitle()` 找到
- 窗口尺寸需要接近 `originSize` 或 `smallSize`
- 屏幕缩放、字体缩放、UI 皮肤变化不能太大
- 鼠标和键盘输入不能被其他程序抢占

这意味着很多 bug 并不是“代码逻辑错误”，而是“运行前提不成立”。

### 15.2 第 1 层：底层运行时

文件：

- `mhxy/__init__.py`
- `pic_target.py`

职责：

- 窗口发现
- 坐标换算
- 图片识别
- 点击/输入
- 日志
- 战斗前后辅助逻辑

这一层的特点是“全局状态很重”。上层任何一个脚本出问题，最终大概率都要回到这里看。

### 15.3 第 2 层：通用任务引擎

文件：

- `mhxy_common.py`

它试图把“任务执行”抽象成一套通用流程：

- 任务栏识别
- 道具使用
- NPC 交互
- 过场处理
- 战斗处理
- 特殊逻辑
- 完成标记

`mhxy_shimeng.py` 已经开始复用这套能力，但仓库里大多数老脚本仍然是各自写死流程，没有统一接到这层。

### 15.4 第 3 层：业务脚本

文件：

- `mhxy_ghost.py`
- `mhxy_fuben.py`
- `mhxy_baotu.py`
- `mhxy_mijing.py`
- `mhxy_dati.py`
- `mhxy_yabiao.py`
- `mhxy_bangpai2.py`
- `mhxy_mine.py`
- 其他 `mhxy_*.py`

这层是真正实现任务行为的地方。大多数 bug 也集中在这里：

- 找图条件不对
- 点击顺序不对
- 未处理某个弹窗
- 超时逻辑缺失
- 战斗/非战斗状态切换判断不稳

### 15.5 第 4 层：调度与入口层

文件：

- `ui/mhxy_pyqt.py`
- `mhxy_richang.py`
- `mhxy_520.py`
- `game_process.py`
- `game_exehelp.py`
- `game_watcher.py`

这层负责：

- 启动脚本
- 串联多个任务
- 调整窗口
- 进程管理
- 登录恢复

它本来应该是“编排层”，但目前也混有不少具体业务判断。

## 16. 关键调用链

后续修 bug 时，最重要的是先判断 bug 落在哪条链路上。

### 16.1 UI 触发链

典型路径：

1. `ui/mhxy_pyqt.py` 按钮点击
2. `exec_script()` 组装命令
3. `os.system("start python[w] ...")`
4. 对应 `mhxy_*.py` 启动
5. 脚本内部调用 `init()`
6. 再进入具体任务循环

这条链的问题是：

- UI 和脚本靠命令字符串拼接耦合
- UI 不知道脚本执行成功还是失败
- 无法拿到脚本实时状态

### 16.2 单任务执行链

典型路径：

1. 入口脚本解析参数
2. `MhxyScript.__init__()` 调用 `init()`
3. `init()` 设置全局 `frame/frameSize/frameSizeCm`
4. 任务脚本调用 `Util.locateCenterOnScreen()` / `Util.locateOnScreen()`
5. 识别成功后执行 `pyautogui` 点击
6. 根据下一张图或下一种状态继续

这条链是仓库最核心的主路径。

### 16.3 聚合任务链

典型路径：

1. `mhxy_richang.py` 或 `mhxy_520.py`
2. 通过 `gotoActivity()` 进入活动
3. 构造子任务对象
4. 复用上一阶段的 `config` 或重新 `init()`
5. 串行跑多个脚本

这条链的风险主要在“上下文串扰”，尤其是多任务连跑时：

- 前一个任务可能留下弹窗
- 窗口状态被改变
- `config` 里的 frame 信息被复用

## 17. 高风险 bug 热点

下面这些不是抽象风险，而是从代码上看就比较像真实缺陷或高概率缺陷点。

### 17.1 UI 目标窗口索引映射不一致

文件：

- `ui/mhxy_pyqt.py`

问题：

- `getTargetArr()` 返回的是 `0,1,2,3,4`
- `getTarget()` 返回的是 `0,1,2,4,5`

影响：

- 单窗口任务和多窗口任务可能打到不同窗口
- 第 4、第 5 个窗口的行为会和预期不一致

这类问题属于“用户点的是 A，脚本打的是 B”，优先级很高。

### 17.2 多选逻辑名义支持，实际不支持

文件：

- `ui/mhxy_pyqt.py`

问题：

- `allChk()` 会把多个目标勾上
- 但 `getTargetArr()` 用的是 `if / elif`
- 最终只会返回第一个被勾中的窗口

影响：

- UI 让用户误以为支持多选
- 实际脚本只执行一个目标

### 17.3 `jingyanlianTask()` 调错脚本

文件：

- `ui/mhxy_pyqt.py`

问题：

- “经验链”按钮实际执行的是 `mhxy_yabiao`

影响：

- 这是直接的功能错误
- 用户从 UI 上完全看不出来

### 17.4 `shopping1Task()` 指向不存在或不一致脚本名

文件：

- `ui/mhxy_pyqt.py`

问题：

- UI 调的是 `mhxy_shopping1`
- 仓库里看到的是 `mhxy_shopping.py`

影响：

- 按钮可能根本无法启动
- 或依赖外部未纳入仓库的脚本

### 17.5 `GameExeHelp.hasExe()` 没有返回值

文件：

- `game_exehelp.py`

问题：

- `hasExe()` 调用了 `hasExe_()` 但没有 `return`

影响：

- `game_watcher.py` 中 `if help.hasExe():` 实际永远拿到 `None`
- 断线监控逻辑分支几乎不可能按预期工作

### 17.6 `GameWatcher` 自身也有多处逻辑未完成

文件：

- `game_watcher.py`

问题：

- `getNextPlayer()` / `getCurPlayer()` 没有返回值
- 登录按钮资源名出现 `jinru_youxi.png.png`
- `while findPicNode is not None:` 里每次又把 `findPicNode = top`
- 整个流程存在明显未完成痕迹

影响：

- 这个模块目前更像草稿，不适合作为稳定能力依赖

### 17.7 `Common` 的旧接口与新引擎已经部分脱节

文件：

- `mhxy_common.py`

问题：

- `addTrigger()` 会往 `self.targets` 里加内容
- 但 `mission()` 现在只遍历 `opTypeTargetListMap`
- `self.targets` / `self.done_pics` 基本已经不生效

影响：

- 调用方以为加了 trigger，实际执行不到
- 很容易出现“代码写了但功能没接上”的隐蔽问题

### 17.8 `Common.Done` 阶段没有显式结束语义

文件：

- `mhxy_common.py`

问题：

- `Done` 只是普通的图片点击
- 找到“完成”图片后不会 `return` 或 `break`

影响：

- 某些任务即使结束了，也可能继续循环、继续找图、继续自动关闭弹窗

### 17.9 `mhxy_shimeng.py` 的 CLI 多窗口参数没有真正生效

文件：

- `mhxy_shimeng.py`

问题：

- 参数是 `idxArray`
- 最终只执行了 `func(int(indexArr[0]))`

影响：

- 看起来支持批量，实际只跑第一个窗口

### 17.10 全局窗口上下文会导致多任务串扰

文件：

- `mhxy/__init__.py`

问题：

- `frame`
- `frameSize`
- `frameSizeCm`

这些都是模块级全局变量。

影响：

- 多窗口串行脚本靠“最后一次 init 的结果”工作
- 任意一个任务重新 `init()`，都可能影响后续任务
- 想做并行或线程化时几乎必然出问题

### 17.11 `gotoActivity()` 和若干等待函数缺少统一超时退出

文件：

- `mhxy/__init__.py`

问题：

- `gotoActivity()` 会长期等待活动入口
- `waitUtilFindPic()` 也默认无限等
- 大量任务主循环是 `while True`

影响：

- 一旦图片失效、界面卡住、弹窗遮挡，脚本只会一直挂住
- 不利于定位是“识别失败”还是“逻辑失败”

### 17.12 日志量过大，且每次找图都打印

文件：

- `mhxy/__init__.py`

问题：

- `Util.locateCenterOnScreen()` 每次调用都 `log()`
- 仓库已有超大 `mhxy_script.log`

影响：

- 实际排查时高价值日志会被噪声淹没
- I/O 会拖慢运行
- 仓库会被日志污染

## 18. 架构性风险

这些问题短期不一定炸，但会直接影响后续新功能开发速度。

### 18.1 业务逻辑和识别策略混写

目前很多脚本把这些内容写在一起：

- 找哪张图
- 图出现说明什么状态
- 该点哪里
- 点完该等多久

结果是后续改一个任务时，往往要同时理解“识别”和“流程”两套逻辑。

### 18.2 坐标系是“隐式上下文”

调用 `Util.leftClick(7, 15)` 时，调用者默认依赖：

- 当前窗口已正确初始化
- 当前窗口尺寸匹配
- 当前 UI 布局和录制时一致

也就是说，代码本身看不到它依赖什么上下文，这会让排查 bug 很慢。

### 18.3 缺少统一任务生命周期

目前不同脚本停止的方式很多：

- `self._flag = False`
- `sys.exit(0)`
- `break`
- `return`
- 抛异常

结果是：

- 很难统一“停止任务”“清理状态”“退出前关闭弹窗”
- UI 也无法知道任务是“完成”“失败”还是“中断”

### 18.4 配置文件读写分散

现在每个任务脚本和每个配置弹窗都自己读取 ini：

- 键名没有统一校验
- 默认值没有集中定义
- 改名或迁移字段时容易漏

这会让配置越来越难维护。

### 18.5 网络控制能力没有边界

文件：

- `mhxy_remote_control.py`
- `mhxy_shopping2.py`
- `mhxy_remote_client.py`

现状：

- 端口写死
- 无鉴权
- 协议简单
- 出错恢复弱

如果未来要继续走远程调度路线，这块需要重新设计，不建议在现状上直接叠功能。

## 19. 新功能应该从哪里接入

后续开发新功能时，不建议“一上来就照着老脚本复制一份”。更稳的方式是先判断任务类型。

### 19.1 适合接到 `Common` 引擎的任务

条件：

- 任务主要由若干固定 UI 状态组成
- 每个状态可以靠图标判断
- 操作基本是点按钮、点任务栏、点确认

这类任务建议：

1. 新建 `resources/<task>/`
2. 新建 `mhxy_<task>.py`
3. 继承 `Common`
4. 通过 `addOpTypeTrigger()` 描述各类状态
5. 在 `mhxy_richang.py` 或 UI 中挂入口

典型例子：

- 师门
- 主线
- 日常里偏线性的交互任务

### 19.2 适合独立脚本实现的任务

条件：

- 任务包含复杂地图移动
- 需要特殊节奏控制
- 需要大量循环、战斗、回合判断

这类任务更适合保留独立逻辑，但建议复用底层公共函数。

典型例子：

- 抓鬼
- 挖矿
- 副本
- 抢购/蹲商品

### 19.3 适合先做“中间层抽象”再接功能的任务

如果你后面准备做以下功能，建议先重构一层再做：

- 任务队列
- 多窗口并行
- 失败自动恢复
- 任务调度面板
- 远程控制中心

因为这些能力会跨多个脚本，如果直接在现有脚本上打补丁，后面会越来越难收。

## 20. 建议的重构优先级

如果目标是“后续能稳定修 bug 和持续加功能”，我建议按下面顺序推进。

### 第一阶段：低风险高收益

- 修 UI 入口错误
- 统一 `.gitignore`
- 控制日志量
- 补 `README`
- 修 `GameExeHelp.hasExe()` 这类直接 bug

收益：

- 立刻降低误操作和调试成本

### 第二阶段：建立可维护边界

- 把窗口上下文封装成 `WindowContext`
- 把配置读写集中到统一模块
- 把任务结果统一为 `success / failed / interrupted / timeout`

收益：

- 后面再加任务时，不用每次都重新发明轮子

### 第三阶段：把业务逻辑做成可组合模块

建议抽出几类对象：

- `Detector`：负责识别图像
- `Action`：负责点击、输入、拖拽
- `TaskStep`：代表一个状态步骤
- `TaskRunner`：负责循环、超时、恢复

收益：

- 可以把“找图”和“业务流程”拆开
- 后续更容易做调试和回放

### 第四阶段：再考虑高级功能

- 多窗口并发
- 任务编排
- 远程队列
- Web/桌面监控面板

## 21. 调试和定位 bug 的建议方法

后面真要修 bug，建议不要一上来就改代码，可以按下面顺序判断。

### 21.1 先判断是哪一类问题

分类方法：

- 资源失效：图找不到
- 坐标失效：图找到了但点错
- 状态机失效：点对了但流程没往下走
- 入口层失效：UI 调错脚本、参数错、窗口错
- 环境失效：窗口尺寸、分辨率、焦点不对

### 21.2 复现时建议记录 4 个信息

- 当前运行的是哪个脚本
- 目标窗口索引是多少
- 当前窗口尺寸是多少
- 卡住时屏幕上实际显示了什么

如果只看日志，不看现场 UI，很多问题很难判断。

### 21.3 最值得先加的调试能力

如果后面要做稳定维护，我最建议先补这两个能力：

- 识别失败时可选保存截图
- 每一步输出“当前状态名”而不是只输出图片路径

这两个能力对定位 bug 的帮助会非常大。

## 22. 面向未来功能开发的建议结构

如果未来打算持续迭代，我建议把新代码逐步往下面这个结构靠拢：

```text
automation/
├─ runtime/         窗口、输入、截图、日志
├─ detectors/       图片模板与识别策略
├─ tasks/           各任务状态机
├─ configs/         配置加载与校验
├─ orchestrators/   日常串联、任务队列、远程调度
└─ ui/              图形界面
```

不需要一次重写，但新功能尽量按这个思路落，会比继续堆在根目录更稳。

## 23. 结论补充

如果只从“能不能跑”看，这个仓库已经有相当多沉淀。
但如果从“后续要长期修 bug 和开发新功能”看，真正的挑战已经不在单个任务脚本，而在下面三件事：

- 入口层是否可靠
- 全局上下文是否可控
- 任务流程是否能被清晰表达和调试

也就是说，后续最有价值的工作，不只是继续加图和加坐标，而是先让项目进入“可诊断、可演进”的状态。
